name: test_oauth2
description: Test web app developed with the use of OAuth's GitHub authentication process as the main target

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  bloc: ^7.1.0
  equatable: ^2.0.3
  flutter_bloc: ^7.2.0
  font_awesome_flutter: ^9.1.0
  shared_preferences: ^2.0.7
  url_launcher: ^6.0.9
  #Las siguientes son necesarias para my_oauth_2
  collection: ^1.15.0
  crypto: ^3.0.0
  http: ^0.13.0
  http_parser: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true